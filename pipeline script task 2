pipeline {
  agent any

  stages {
    stage('Run Nginx pod with ansible ') {
      steps {
        ansiblePlaybook credentialsId: 'ssh', disableHostKeyChecking: true, installation: 'Ansible', inventory: '/etc/ansible/hosts', playbook: '/home/rihab/deploy_nginx.yml'
        }
    }
  }
}
